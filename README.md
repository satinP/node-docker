## First use:
### run commands: 
 - docker build -t node-docker .   
 - docker run -p 3000:3000    
 - docker run -p 3000:3000 -d node-docker

## After first use:
### run command: 
 - docker-compose up  
